apiVersion: v1
kind: Service
metadata:
  name: {{ include "contextmine.fullname" . }}-api
  labels:
    {{- include "contextmine.api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "contextmine.api.selectorLabels" . | nindent 4 }}
